#include "holberton.h"

/**
* _isdigit - checks to see if digit
*
* @c: input
*
* Return : result
*/
int _isdigit(int c)
{
	int r;

	if (c >= 48 && c <= 57)
{
	r = 1;
}
	else if (c >= 48 && c <= 57)
{
	r = 1;
}
	else
{
	r = 0;
}
	return (r);
}
